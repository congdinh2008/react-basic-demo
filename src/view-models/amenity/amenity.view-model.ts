import { MasterBaseViewModel } from "../master-base.view-model";

export class AmenityViewModel extends MasterBaseViewModel {
    public name!: string;
    public description!: string;
    public price!: number;
}